package com.radiopirate.lib.service;

// Enum of actions for which the app provides a list of app specific values.
// Need app specific values to ensure app don't conflict with each others
public enum ServiceActions {
    STOP, PLAY, PAUSE, UPDATE, DOWNLOAD_PODCAST, STATUS_UPDATE, PLAY_PAUSE, NEXT, PREVIOUS
}
